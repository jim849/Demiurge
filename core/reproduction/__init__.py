"""Replaceable reproduction module (iron law 5).

Owns ONLY the genetics of reproduction -- how a child's genome derives from its
parent(s). The *when* (energy threshold), *how much* (energy transfer), and *where*
(offspring placement) are the World's job, in its resolve phase. v1 ships asexual
copy+mutate; M3 adds sexual recombination by swapping the strategy, World untouched.
"""
